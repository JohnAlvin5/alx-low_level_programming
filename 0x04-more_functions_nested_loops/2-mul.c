#include "main.h"

/**
 * mul - writes the character c to stdout
 * @a: First number
 * @b: Second number
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	int m;

	m = a * b;

	return (m);
}
